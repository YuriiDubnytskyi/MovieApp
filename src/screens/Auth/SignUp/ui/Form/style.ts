import { StyleSheet } from 'react-native';
import { wp } from '../../../../../helpers/responsive';
import { WP_ONE_DP } from '../../../../../constants/styles';

export default StyleSheet.create({
  form: {
    width: '100%',
    marginTop: wp(WP_ONE_DP * 20),
  },
});
