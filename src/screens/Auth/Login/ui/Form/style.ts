import { StyleSheet } from 'react-native';
import { hp } from '../../../../../helpers/responsive';
import { HP_ONE_DP } from '../../../../../constants/styles';

export default StyleSheet.create({
  form: {
    width: '100%',
    marginTop: hp(HP_ONE_DP * 20),
  },
});
